## Barangay Management System (BMS)
For MITACOR
### How tu run
Softwares needed
    composer
    Laravel 9
    XAMPP
    VS Code

Install dependencies using: 
    npm install
    composer update && composer install

run using:
    npm run dev
    php artisan serve